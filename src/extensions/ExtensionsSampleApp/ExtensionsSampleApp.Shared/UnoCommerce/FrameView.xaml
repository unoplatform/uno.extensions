<UserControl x:Class="ExtensionsSampleApp.UnoCommerce.FrameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:ExtensionsSampleApp.UnoCommerce"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:nav="using:Uno.Extensions.Navigation.Controls"
             d:DesignHeight="300"
             d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <CommandBar>
            <AppBarButton Icon="Back"
                          Label="Back"
                          nav:Navigation.Request="./-" />
            <CommandBar.Content>
                <TextBlock Text="Title"
                           Margin="12,8" />
            </CommandBar.Content>
        </CommandBar>
        <Frame nav:Region.Attached="True" Grid.Row="1" />
    </Grid>
</UserControl>
