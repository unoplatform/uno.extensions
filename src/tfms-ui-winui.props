<!--
	TFMS for the shipped libraries that reference Uno.WinUI (i.e. WinUI).

	Uno.WinUI 7.0 ships library assets only for net10.0 and net10.0-windows10.0.19041, but the android and
	ios flavours still build against its net10.0 asset, and Uno 7 keeps a library's own android/ios build on
	device. Those flavours carry the platform code (KeyStore/Keychain storage, native HTTP handlers, mobile
	logging sinks), so they stay. App heads use tfms-ui-winui-apps.props instead.
-->
<Project>
	<PropertyGroup Condition="'$(UnoTargetFrameworkOverride)'!=''">
		<TargetFrameworks>$(UnoTargetFrameworkOverride)</TargetFrameworks>
	</PropertyGroup>
	<PropertyGroup  Condition="'$(UnoTargetFrameworkOverride)'==''">
		<TargetFrameworks Condition="'$(Build_iOS)'=='true'">$(TargetFrameworks);net10.0-ios</TargetFrameworks>
		<TargetFrameworks Condition="'$(Build_Android)'=='true'">$(TargetFrameworks);net10.0-android</TargetFrameworks>
		<TargetFrameworks Condition="'$(Build_Windows)'=='true'">$(TargetFrameworks);net10.0-windows10.0.19041</TargetFrameworks>
		<TargetFrameworks>$(TargetFrameworks);net10.0</TargetFrameworks>
	</PropertyGroup>
	<PropertyGroup>
		<WindowsSdkPackageVersion>10.0.19041.57</WindowsSdkPackageVersion>
	</PropertyGroup>
</Project>
