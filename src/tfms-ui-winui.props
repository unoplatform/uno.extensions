<!--
	TFMS for the shipped libraries that reference Uno.WinUI (i.e. WinUI).

	Uno Platform 7.0 renders with Skia everywhere, so Uno.WinUI ships library assets only for net10.0 and
	net10.0-windows10.0.19041 - there is no longer an ios/android/maccatalyst/browserwasm flavour to bind
	against. App heads keep targeting their platform; they use tfms-ui-winui-apps.props instead.
-->
<Project>
	<PropertyGroup Condition="'$(UnoTargetFrameworkOverride)'!=''">
		<TargetFrameworks>$(UnoTargetFrameworkOverride)</TargetFrameworks>
	</PropertyGroup>
	<PropertyGroup  Condition="'$(UnoTargetFrameworkOverride)'==''">
		<TargetFrameworks Condition="'$(Build_Windows)'=='true'">$(TargetFrameworks);net10.0-windows10.0.19041</TargetFrameworks>
		<TargetFrameworks>$(TargetFrameworks);net10.0</TargetFrameworks>
	</PropertyGroup>
	<PropertyGroup>
		<WindowsSdkPackageVersion>10.0.19041.57</WindowsSdkPackageVersion>
	</PropertyGroup>
</Project>
