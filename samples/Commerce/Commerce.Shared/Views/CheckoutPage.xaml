<Page
    x:Class="Commerce.CheckoutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Commerce"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      xmlns:nav="using:Uno.Extensions.Navigation.Controls"
      xmlns:toolkit="using:Uno.Toolkit.UI.Controls"
      Background="{ThemeResource MaterialBackgroundBrush}">

    <Grid  MinHeight="300"
           MinWidth="300">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <toolkit:NavigationBar Content="Checkout" />
        <StackPanel Grid.Row="1">
            <TextBlock Text="Checkout"
                       FontSize="32"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center" />
			<Button Content="Done"
					nav:Navigation.Request="../../-" />
		</StackPanel>

    </Grid>
</Page>
