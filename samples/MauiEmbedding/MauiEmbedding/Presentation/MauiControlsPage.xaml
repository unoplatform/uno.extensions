<Page
	x:Class="MauiEmbedding.Presentation.MauiControlsPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:embed="using:Uno.Extensions.Maui"
	xmlns:local="using:MauiEmbedding.Presentation"
	xmlns:maui="using:Microsoft.Maui.Controls"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:uen="using:Uno.Extensions.Navigation.UI"
	xmlns:utu="using:Uno.Toolkit.UI"
	Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
	NavigationCacheMode="Required"
	mc:Ignorable="d">

	<Page.Resources>
		<ResourceDictionary>
			<x:String x:Key="HelloWorld">Hello from WinUI Resources</x:String>
			<Color x:Key="DemoColor">#169420</Color>
			<local:TitleConverter x:Key="TitleConverter" />
		</ResourceDictionary>
	</Page.Resources>

	<Grid utu:SafeArea.Insets="All">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition />
		</Grid.RowDefinitions>
		<utu:NavigationBar Content="{Binding Title}" />

		<embed:MauiContent Grid.Row="1">
			<maui:ScrollView>
				<maui:ScrollView.Content>
					<maui:VerticalStackLayout x:Name="stack" Spacing="16">

						<maui:Label Text="{embed:MauiResource Key=HelloWorld}" TextColor="{embed:MauiResource Key=DemoColor}" />
						<!--<maui:BoxView HeightRequest="300" />-->
						<!--<maui:Label Text="{embed:MauiResource Key=HelloWorld}" TextColor="{embed:MauiResource Key=DemoColor}" />
		
						
		<maui:Label x:Name="lbl" Text="{embed:MauiBinding Path=Title, Converter={StaticResource TitleConverter}}" />-->

					</maui:VerticalStackLayout>
				</maui:ScrollView.Content>
			</maui:ScrollView>
		</embed:MauiContent>

		<StackPanel
			Grid.Row="1"
			HorizontalAlignment="Center"
			VerticalAlignment="Center"
			Spacing="16" />
	</Grid>
</Page>
